<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.sieric.thehat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.OnlineGameManageWordsActivity"></activity>
        <activity android:name=".activities.OnlineGameDictionaryActivity" />
        <activity android:name=".activities.OnlineGameMenuActivity" />
        <activity android:name=".activities.ManageDictionaryActivity" />
        <activity android:name=".activities.OnlineGameManagePlayersActivity" />
        <activity android:name=".activities.OnlineGameChooseActivity" />
        <activity android:name=".activities.GameStatisticsActivity" />
        <activity android:name=".activities.DictionaryListActivity" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".activities.ManagePlayersActivity" />
        <activity android:name=".activities.OfflineGameMenuActivity" />
        <activity android:name=".activities.DictionaryChoiceActivity" />
        <activity android:name=".activities.GameCountdownActivity" />
        <activity android:name=".activities.GameActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ocrreader.OcrCaptureActivity" />
    </application>

</manifest>