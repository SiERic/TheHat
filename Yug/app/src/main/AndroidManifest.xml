<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.sieric.thehat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.game.OneToOthersGameActivity" />
        <activity android:name=".activities.onlineGame.AddWordsActivity" />
        <activity android:name=".activities.onlineGame.DictionaryListActivity" />
        <activity android:name=".activities.onlineGame.MenuActivity" />
        <activity android:name=".activities.dictionaryManagement.ManageDictionaryActivity" />
        <activity android:name=".activities.onlineGame.ManagePlayersActivity" />
        <activity android:name=".activities.onlineGame.CreateJoinActivity" />
        <activity android:name=".activities.game.StatisticsActivity" />
        <activity android:name=".activities.dictionaryManagement.DictionaryListActivity" />
        <activity
            android:name=".activities.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".activities.offlineGame.ManagePlayersActivity" />
        <activity android:name=".activities.offlineGame.MenuActivity" />
        <activity android:name=".activities.offlineGame.DictionaryListActivity" />
        <activity android:name=".activities.game.CountdownActivity" />
        <activity android:name=".activities.game.GameActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ocrreader.OcrCaptureActivity" />
    </application>

</manifest>